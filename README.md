# 날씨 앱

대한민국 행정구역 기반 날씨 정보 조회 앱입니다. 삼성 갤럭시 날씨 앱 스타일의 몰입감 있는 UI를 제공합니다.

**배포 URL**
https://weather.kimgh06.com
---

## 프로젝트 실행 방법

### 환경 변수 설정

루트 디렉토리에 `.env.local` 파일을 생성하고 아래 값을 설정합니다.

```
VITE_OWM_API_KEY=<OpenWeatherMap API Key>
```

- `VITE_USE_MOCK=true`로 설정하면 API 키 없이 목(mock) 데이터로 실행할 수 있습니다.

### 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 미리보기
npm run preview
```

### 테스트

```bash
npm test          # watch 모드
npm run test:run  # 단회 실행
```

---

## 구현한 기능

### 1. 현재 위치 날씨 조회

- 앱 첫 진입 시 브라우저 Geolocation API로 사용자의 현재 위치를 자동 감지합니다.
- 위치 권한 거부 시 서울특별시를 기본 위치로 사용합니다.
- **현재 기온**, **당일 최저/최고 기온**, 체감온도, 습도, 풍향·풍속, 기압, 가시거리를 표시합니다.
- 현재 날짜·요일·시각을 표시하며 1분마다 자동 갱신됩니다.

### 2. 날씨 상세 정보

- **시간대별 기온**: 3시간 간격 최대 8개 슬롯(+ 현재 "지금" 포함)의 기온·날씨 아이콘·강수확률 수평 스크롤 표시
- **주간 예보 (5일)**: OWM Forecast 데이터를 현지 시간대 기준 날짜로 그룹핑, 일별 최저/최고 기온 범위 바 차트
- **대기질**: PM2.5·PM10 농도를 **환경부 기준**으로 각각 좋음/보통/나쁨/매우나쁨 4등급 색상 시각화
- **일출/일몰 아크**: 현재 시각 기준 태양 위치를 호(arc) 형태로 시각화, 시각은 해당 도시 현지 시간 표시
- **풍향 나침반**: SVG 나침반으로 풍향·풍속 시각화
- **℃/℉ 토글**: 메인 기온 탭으로 단위 전환, 전체 온도 표시에 즉시 반영되며 설정이 localStorage에 저장됨
- **상세 지표**: 가시거리, 기압

### 3. 장소 검색

- **한국어 입력**: 제공된 `korea_districts.json` 기반 클라이언트 사이드 검색으로 시·도, 구·군, 동 단위 즉시 응답
- **해외 도시**: 영문 입력 시 OWM Geocoding API(`/geo/1.0/direct`)를 호출해 전 세계 도시 검색 가능 (예: "Tokyo", "New York")
- 한국어 IME 조합 중에도 실시간 검색 트리거 (compositionstart/end 처리)
- 날씨 정보가 없는 장소는 **"해당 장소의 정보가 제공되지 않습니다."** 메시지를 표시합니다.
- 최근 검색어를 localStorage에 저장하여 빠른 재접근이 가능합니다.
- `/` 키보드 단축키로 검색창을 즉시 열 수 있습니다.

### 4. 즐겨찾기

- 현재 조회 중인 장소를 별 아이콘으로 즐겨찾기에 **추가/삭제**할 수 있습니다.
- **최대 6개**까지 추가 가능하며, 초과 시 토스트 알림으로 안내합니다.
- 즐겨찾기 카드에는 날씨 상태별 그라데이션 배경과 함께 **현재 날씨, 당일 최저/최고 기온**을 표시합니다.
- **그리드/리스트 뷰 전환** 버튼으로 카드 레이아웃을 변경할 수 있으며, 설정이 localStorage에 저장됩니다.
- 카드 클릭 시 해당 장소의 **상세 날씨 페이지**로 이동합니다.
- 즐겨찾기 장소의 **별칭(이름)을 수정**할 수 있습니다 (연필 아이콘 클릭 또는 카드 롱프레스).
- **좌로 스와이프**하여 삭제할 수 있습니다.
- 삭제 후 5초 내 **실행 취소** 토스트 버튼으로 복원할 수 있습니다.
- `localStorage`를 활용하여 즐겨찾기 데이터를 영구 저장합니다.
- 새로고침 후에도 현재 위치가 즐겨찾기에 등록된 경우 별 아이콘 상태가 유지됩니다.

### 5. 사이드바 네비게이션

- 좌측 슬라이드 사이드바에서 즐겨찾기 목록을 확인하고 빠르게 이동할 수 있습니다.
- 현재 위치로 돌아가거나 검색창을 바로 열 수 있습니다.

### 6. UX / 반응형

- **반응형 디자인**: 모바일(375px 기준)·데스크탑 모두 지원, 즐겨찾기 그리드 2열/3열 전환
- **동적 배경**: 날씨 상태(맑음/흐림/비/눈/안개)와 낮·밤에 따라 배경 그라디언트가 자동 전환됩니다.
- **스켈레톤 로딩**: 데이터 로딩 중 레이아웃 시프트 없는 스켈레톤 UI
- **에러 카드**: 데이터 로드 실패 시 재시도 버튼 제공
- **Pull-to-refresh**: 모바일에서 아래로 당겨 날씨 데이터를 새로고침
- **자동 갱신**: 날씨 5분 · 대기질 10분 간격
- **PWA**: 홈 화면 추가 및 Service Worker 오프라인 캐싱 지원
- **오프라인 배너**: 네트워크 연결 끊김 시 안내 표시

---

## 기술적 의사결정 및 이유

### FSD (Feature Sliced Design) 아키텍처

```
src/
├── app/          # 앱 초기화, Provider, 라우팅, 글로벌 스타일
├── pages/        # 라우트 단위 페이지 (Home, Detail, Favorites)
├── widgets/      # 독립 UI 블록 (WeatherDisplay, SearchBar, Sidebar, AirQualityCard, SunArcCard)
├── features/     # 사용자 인터랙션 (위치 감지, 장소 검색, 즐겨찾기 관리)
├── entities/     # 비즈니스 엔티티 (weather, location)
└── shared/       # 공용 유틸리티, UI 컴포넌트, 설정, 검색 인덱스
```

레이어 간 단방향 의존성(`app → pages → widgets → features → entities → shared`)을 강제해 변경의 영향 범위를 명확히 격리합니다. 기능 추가·수정 시 관련 슬라이스만 건드리면 됩니다.

### 장소 검색: 로컬 + 글로벌 이중 전략

한국어 입력은 `korea_districts.json`을 번들에 포함해 네트워크 없이 즉각 응답합니다. 세그먼트 기반 점수 알고리즘(완전 일치 100점 → 부분 포함 50점)으로 관련도 순 정렬, TTL 30초 인메모리 캐시로 반복 검색 성능을 최적화했습니다. 영문 등 비한국어 입력은 OWM Geocoding API를 TanStack Query로 호출해 해외 도시를 검색합니다. 두 전략을 입력 언어 감지(`/[\uAC00-\uD7A3]/`)로 자동 분기해 불필요한 API 호출을 방지합니다.

### TanStack Query 데이터 관리

- `queryKey: ["weather", lat, lon]`으로 위치별 날씨를 독립적으로 캐싱합니다.
- `refetchInterval` 5분/10분으로 자동 갱신을 구현해 항상 최신 데이터를 유지합니다.
- 동일 쿼리 키를 공유하므로 즐겨찾기 카드에서 중복 API 호출이 발생하지 않습니다.
- `select` 옵션으로 즐겨찾기 카드에서 요약 데이터만 구독합니다(`useWeatherSummary`).
- 날씨와 대기질 쿼리를 분리해 일부 실패 시 나머지 정보는 정상 표시됩니다.

### API 키 보안 (프록시 구조)

`/api/owm/*` 경로를 Vite 개발 서버 프록시로 OpenWeatherMap에 포워딩합니다. API 키가 클라이언트 번들에 노출되지 않으며, 프로덕션 배포 시 동일한 구조로 서버사이드 프록시를 구성합니다.

### 즐겨찾기 Soft Delete (실행 취소)

삭제 요청 시 상태에서 즉시 제거하되 5초간 `pendingRef`에 보관합니다. 타임아웃 만료 전 "실행 취소" 클릭 시 원래 인덱스에 복원합니다. 확인 다이얼로그 없는 빠른 조작과 안전망을 동시에 제공합니다.

### 즐겨찾기 현재 위치 식별: 좌표 허용 오차

`isFavorite(lat, lon)` 비교에 `===` 엄격 동등을 쓰면 GPS 좌표가 호출마다 수 미터씩 달라지는 특성 때문에 새로고침 후 동일 위치를 다른 위치로 판단합니다. 0.001° (약 100m) 허용 오차를 적용해 GPS 드리프트를 흡수하면서 인접 행정구역과의 혼동은 방지합니다.

### 국제 시간대 처리

OWM Current Weather 응답의 `timezone` 필드(UTC 오프셋, 초 단위)를 `WeatherData`에 전파합니다. `toLocalDate(utcUnix, offsetSec)` 유틸로 현지 날짜·시각을 계산하며, `Date.getUTC*()` 메서드로 읽어 외부 라이브러리 없이 처리합니다. 하드코딩된 `Asia/Seoul`을 제거해 전 세계 모든 도시의 시간대를 정확히 표시합니다.

### 대기질: 한국 환경부 기준 적용

OWM Air Pollution API의 AQI는 유럽 기준이라 한국 사용자에게 부적합합니다. PM2.5·PM10 원시 농도값(`µg/m³`)을 환경부 예보 기준으로 직접 등급화하여 실제 한국 대기질 앱과 동일한 기준을 제공합니다.

### OpenWeatherMap API 활용

- Current Weather API 단일 응답에서 sunrise/sunset, pressure, visibility, timezone 등 추가 데이터를 파싱해 **추가 호출 없이** 상세 정보를 확장합니다.
- 5-Day/3-Hour Forecast의 40개 포인트를 해당 도시의 현지 시간대 기준 날짜로 그룹핑해 주간 예보를 도출합니다.

---

## 사용 기술 스택

| 기술 | 버전 | 용도 |
|------|------|------|
| React | 19 | UI 라이브러리 (Functional Component) |
| TypeScript | 5 | 타입 안전성 |
| Vite | 6 | 빌드 도구 |
| React Router DOM | 6 | SPA 라우팅 |
| TanStack Query | 5 | 서버 상태 관리 및 캐싱 |
| Tailwind CSS | 4 | 스타일링 |
| Lucide React | - | 아이콘 |
| vite-plugin-pwa | - | PWA 지원 |
| Vitest + Testing Library | - | 단위 테스트 |
| OpenWeatherMap API | - | 날씨 · 대기질 데이터 |
| FSD | - | 아키텍처 패턴 |
