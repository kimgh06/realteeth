export interface District {
  code: string;
  name: string;
  lat: number;
  lon: number;
}

export interface Favorite {
  id: string;
  name: string;
  alias: string;
  lat: number;
  lon: number;
}
